@model UsuarioModel
@{
    ViewData["Title"] = "Apagar registro";
}
<div class="text-center">
    <h1 class="display-4">Apagar usuário:</h1>
</div>
<br />
<div class="card shadow p-3 mb-5 bg-white rounded" style="width:50%; margin: auto;">
    <form method="post" asp-controller="Usuario" asp-action="Excluir">
        <div class="d-flex">
            <input type="hidden" asp-for="Id"/>
            <div>
                <div class="form-group">
                    <label class="picture_show" for="picture_upload" tabindex="0">
                        <img src="@Model.GetImagePath()" id="foto_perfil" onerror="this.onerror=null; this.src='/img/sem-foto.jpeg'">
                    </label>
                </div>
            </div>

            <div class="boxInputs" style="margin-top:40px;">
                <div class="form-group">
                    <label asp-for="Nome">Nome: @Model.Nome</label>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Email">E-mail: @Model.Email</label> 
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Tipo_Usuario">Tipo usuário: @Model.Tipo_Usuario</label>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Login">Login: @Model.Login</label>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="DataCadastro">Data de cadastro: @Model.DataCadastro</label>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="DataAlteração">Ultima alteração: @Model.DataAlteração</label>
                </div>
                <br />
            </div>
        </div>

        <br />
        <div>
            <p>
                Realmente deseja apagar esse usuário:
                <div class="radio-buttons-container">
                    <div class="radio-button">
                        <input name="radio-group" id="rdSim" class="radio-button__input" type="radio">
                        <label for="rdSim" class="radio-button__label">
                            <span class="radio-button__custom"></span>
                            Sim
                        </label>
                    </div>
                    <div class="radio-button">
                        <input name="radio-group" id="rdNao" class="radio-button__input" type="radio" checked>
                        <label for="rdNao" class="radio-button__label">
                            <span class="radio-button__custom"></span>
                            Não
                        </label>
                    </div>

                </div>
            </p>
            <button type="submit" class="btn btn-danger">Apagar</button>
            <a class="btn btn-secondary" asp-controller="Usuario" asp-action="Index">Cancelar</a>
        </div>
    </form>
</div>

